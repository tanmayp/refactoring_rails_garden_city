<p id="notice"><%= notice %></p>

<p>
  <strong>User Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Account Name:</strong>
  <%= Account.for_user(@user).name %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
